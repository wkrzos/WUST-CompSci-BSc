let split3Rec list =
1. calculate list length
2. get /3 with no remainder + the number must be divisible by 3
3. save the number of elements in one smaller  list
3. recursive method, match/if, 
;;

let split3Tail list =
1
;;